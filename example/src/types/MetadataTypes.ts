export type VideoTrackMetadata = {
  type: string;
  active: boolean;
};

export type AudioTrackMetadata = {
  type: string;
  active: boolean;
};

export type ParticipantMetadata = {
  displayName: string;
};
